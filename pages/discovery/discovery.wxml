
<!--pages/discovery/discovery.wxml-->
<import src="/utils/template.wxml"/>
<view class="page">
    <view class="page__hd">
         <view class="location ellipsis" bindtap="toNearby">
			<image class="icon" src="../../images/icon_location.png"/>
			{{cityName}}
	    </view>
    </view>
   <view class="weui-flex">
    <view class="weui-flex__item" style="text-align:center">
    <picker bindchange="bindPickerChange" value="{{index}}" range="{{typeArray}}">
    <view class="picker">
      全部 <image style="width: 8px; height: 8px;" src="/images/arrow-down.png" background-size="cover">
        </image>
    </view>
  </picker>
  
    </view>
    <view class="weui-flex__item" style="text-align:center">
    |
    </view>
    <view class="weui-flex__item" style="text-align:center">
    <picker bindchange="bindPickerChange" value="{{index}}" range="{{allArray}}">
    <view class="picker">
      综合 <image style="width: 8px; height: 8px;" src="/images/arrow-down.png" background-size="cover">
        </image>
        
    </view>
  </picker>
    </view>
    <view class="weui-flex__item" style="text-align:center">
    |
    </view>
    <view class="weui-flex__item" style="text-align:center">
    <picker bindchange="bindPickerChange" value="{{index}}" range="{{allArray}}">
    <view class="picker">
      筛选 <image style="width: 8px; height: 8px;" src="/images/arrow-down.png" background-size="cover">
        </image>
        
    </view>
  </picker>
    </view>
</view>

    <!--<view class="choiceType">
       <view> 
            <picker  bindchange="bindSprotChange" value="{{index1}}" range="{{sportsArray}}">
            <button class="picker" size="mini">{{sportsArray[index1]}} </button>
            </picker>
        </view>-->
        <!--<view>
            <picker  bindchange="bindDisChange" value="{{index2}}" range="{{distanceArray}}">
            <view class="picker">距离选择:{{distanceArray[index2]}}</view>
            </picker>
      
       </view>
       
    </view>-->


    <view class="page__bd">
        <scroll-view scroll-y="true"  style="height:{{scrollHeight}}px;" bindscrolltolower="loadMore" bindscrolltoupper="doFresh">

	    <view class="page_list" wx:for="{{markers}}" wx:for-index="id">
	    <template is="tPlaceList" data="{{item}}"/>	 
	    </view>

        <view class="tips1">
      <view wx:if="{{hasMore}}" style="display: flex;flex-direction: row;align-items: center;align-self: center;justify-content: center;">
      <icon type="waiting" size="45"/><text>玩命的加载中...</text></view>
      <view wx:else><text>没有更多内容了</text></view>
    </view>
</scroll-view>
    </view>
   
</view>
